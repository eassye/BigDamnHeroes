# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '8.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    ng build
  displayName: 'npm install and build'

- script: ng lint
  displayName: 'Lint Validation'

- task: AzureRmWebAppDeployment@3
  inputs:
    azureSubscription: 'Free Trial (80f53a47-2930-4729-96aa-ce5c8268fa27)'
    WebAppName: 'BigDamnHeroes'
    Package: '$(System.DefaultWorkingDirectory)'
    GenerateWebConfig: true
    WebConfigParameters: '-Handler iisnode -NodeStartFile server.js -appType node'

# Will need to run unit test using mocha and chai, azure agent can not open chrome. 
# - script: ng test
#   displayName: 'Unit Test'

# - script: ng e2e
#   displayName: 'End to End UI Test'
